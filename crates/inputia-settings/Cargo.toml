[package]
name = "inputia-settings"
version = "0.0.1"
edition = "2021"
publish = false

[lib]
name = "inputia_settings"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
